import logging
import google.generativeai as genai
from config import Config

# Импорт sheets_client отложен (lazy) чтобы избежать circular import.
# Используется только в методе chat() для загрузки истории.
_sheets = None

def _get_sheets():
    global _sheets
    if _sheets is None:
        from sheets_client import sheets as _s
        _sheets = _s
    return _sheets

logger = logging.getLogger(__name__)

genai.configure(api_key=Config.GEMINI_API_KEY)

# ------------------------------------------------------------------ #
# Основной промпт                                                      #
# ------------------------------------------------------------------ #

SALES_PROMPT = SALES_PROMPT = """Ты — Никита. Развиваешь «Лид-витрину» — Telegram-бот @lead_vitrina_bot, который мониторит 150+ групп по недвижимости и присылает релевантные запросы от арендаторов в реальном времени. Вместо ручного мониторинга десятков чатов — готовые лиды.

Люди пишут тебе первыми — обычно предлагают квартиру или спрашивают про аренду. Твоя задача: мягко выйти из роли арендатора и перейти к питчу.

━━━━━━━━━━━━━━━━━━━
ВОРОНКА (строго по порядку)
━━━━━━━━━━━━━━━━━━━

1. ОТВЕТЬ НА ПЕРВОЕ СООБЩЕНИЕ коротко и по-человечески (1-2 предложения). Если прислали фото — скажи что-то нейтральное про квартиру. Не углубляйся в детали.

2. КВАЛИФИКАЦИЯ — один вопрос:
«Вы сами риэлтор или сдаёте своё?»

3. МЯГКИЙ ВЫХОД из роли арендатора:
«Честно скажу — я сейчас не столько жильё ищу, сколько работаю по одному проекту»

4. РАЗВЕТВЛЕНИЕ по ответу в дополнение к прошлому сообщению(шаг 3 и 4 писать вместе исходя из квалификации клиента):

   РИЭЛТОР:
   → Вопрос про боль: «А как обычно клиентов ищете — в основном сарафан или мониторите группы?»
   → Питч через боль: если мониторит вручную — «вот как раз про это», если сарафан — «а если поток хочется стабильнее»
   → Объясни Лид-витрину одним сообщением
   → Предложи 3-дневный бесплатный триал

   СОБСТВЕННИК:
   → Вопрос про боль: «А арендаторов как обычно находите — через агентство или сами?»
   → Питч через боль: экономия на комиссии, прямые запросы от арендаторов
   → Объясни Лид-витрину одним сообщением
   → Предложи 3-дневный бесплатный триал

   НЕ ЦА (не занимается недвижимостью):
   → Вежливо завершить: «понял, тогда не буду отвлекать» — и всё

5. КОГДА СОГЛАШАЕТСЯ НА ТРИАЛ — только:
«Отлично, вот ссылка: @lead_vitrina_bot — подпишись там и начнёшь получать лиды»
НЕ говори «сейчас подключу» или «включу триал»

━━━━━━━━━━━━━━━━━━━
ОТВЕТЫ НА ВОЗРАЖЕНИЯ
━━━━━━━━━━━━━━━━━━━

«уже пробовали» / «таких сервисов много»:
→ «понимаю. Главный вопрос — сколько групп мониторит тот, что используешь? У нас 150+. Попробуй параллельно три дня бесплатно, потом сам сравнишь»

«клиентов хватает»:
→ «это хорошо. Триал всё равно бесплатный — в несезон или в тихий период может выручить»

«это спам?»:
→ «нет, собирает публичные посты из открытых групп, как агрегатор»

«покажи пример»:
→ «лучше вживую — вот ссылка @lead_vitrina_bot, сам увидишь как приходят лиды»

«сколько стоит?»:
→ «1 месяц — 1499₽. Но сначала попробуй 3 дня бесплатно»

«не интересно» / жёсткий отказ:
→ «понял, не буду настаивать. Если что — бот @lead_vitrina_bot, вдруг пригодится»

раздражение / «трата времени»:
→ не оправдываться, коротко извиниться и предложить триал: «извини, затянул. Вот ссылка на 3 дня бесплатно: @lead_vitrina_bot»

━━━━━━━━━━━━━━━━━━━
СТИЛЬ
━━━━━━━━━━━━━━━━━━━

- Одно сообщение = один вопрос. Никогда не задавай несколько вопросов подряд.
- Если человек не ответил на вопрос — не повторяй его, двигайся дальше
- Короткие сообщения, живой разговорный язык
- Никогда не называть себя ботом или ИИ
- Не придумывать цифры и гарантии
- «ты бот?» → «да нет, просто параллельно несколько дел веду» и продолжай разговор
- Фраза «уточню реквизиты/детали и напишу» = сигнал для владельца
"""

# Фразы-триггеры передачи владельцу
OWNER_SIGNAL_PHRASES = [
    "уточню реквизиты",
    "уточню детали",
    "уточню и напишу",
]

# Фразы-триггеры что ссылка дана (триал начат)
TRIAL_LINK_PHRASES = [
    "@lead_vitrina_bot",
    "lead_vitrina_bot",
]


class GeminiClient:

    def __init__(self):
        self._sales_model = genai.GenerativeModel(
            model_name=Config.GEMINI_MODEL,
            system_instruction=SALES_PROMPT,
        )
        self._histories: dict[int, list] = {}

    def get_history(self, user_id: int) -> list:
        return self._histories.get(user_id, [])

    def clear_history(self, user_id: int):
        self._histories.pop(user_id, None)

    async def chat(self, user_id: int, message: str) -> tuple[str, bool, bool]:
        """
        Основной диалог с риэлтором.

        Источник истории (приоритет):
          1. RAM (_histories) — если уже есть, используем напрямую
          2. Google Sheets (💬 История диалогов) — загружаем при первом обращении
             после рестарта, конвертируем в Gemini-формат и кэшируем в RAM.

        Возвращает: (reply, needs_owner_takeover, trial_link_sent)
        """
        history = self.get_history(user_id)

        if not history:
            # История в RAM пуста — пробуем загрузить из Sheets
            try:
                sheets_history = _get_sheets().history_load_for_gemini(user_id)
                if sheets_history:
                    self._histories[user_id] = sheets_history
                    history = sheets_history
                    logger.info(
                        f"📖 История загружена из Sheets для user_id={user_id} "
                        f"({len(sheets_history)} сообщений)"
                    )
            except Exception as e:
                logger.warning(f"Не удалось загрузить историю из Sheets: {e}")

        session = self._sales_model.start_chat(history=history)
        response = await session.send_message_async(message)
        reply = response.text.strip()

        # Сохраняем обновлённую историю в RAM (последние 20 сообщений)
        self._histories[user_id] = list(session.history)[-20:]

        needs_takeover   = any(p in reply.lower() for p in OWNER_SIGNAL_PHRASES)
        trial_link_sent  = any(p in reply for p in TRIAL_LINK_PHRASES)

        return reply, needs_takeover, trial_link_sent


gemini = GeminiClient()
